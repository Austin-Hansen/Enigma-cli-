
#include "machine.h"
#include <string>
    Machine::Machine() { }

       const std::array<char,27> alphabet = {"ABCDEFGHIJKLMNOPQRSTUVWXYZ"};

       Rotor rotor;
       Rotor rotor1;
       Rotor rotor2;
       Rotor rotor3;

      char* word(char input[]){
          int size = *(&input+1)-input;
         char output[size];
        for(int i=0; i<size;i++){
           output[i]= run(input[i])
        }
         return output;
       }

    char run(char in){

    int inputnum = alphabet.find(in);
    }

       void Machine::setRot(int R,int M,int L){
        if (R != M && R!= L && M != L){
       rotor1=Rotor(R,1);
       rotor2=Rotor(M,2);
       rotor3=Rotor(L,3);
         }
       }
    void Machine::setRotpos(int R,int M,int L){
       rotor1.position=R;
       rotor2.position=M;
       rotor3.position=L;
    }
    void turn(){
       rotor1.position++;
      if (rotor1.position == 26){
        rotor1.position = 0;
        rotor2.position+=1;
       if (rotor2.position == 26){
         rotor2.position = 0;
         rotor3.position+=1;
        if(rotor3.position == 26){
          rotor3.position = 0;
       }
      }
     }
    }

